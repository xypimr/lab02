#include <iostream>
#include <string>


int main(){
    std::string name; 
    std::cin >> name; // Inputing name from console
    std::cout << "Hello world from " << name << std::endl; // Outputing text with name
    // new comment to conflict
    return 0; 
}
